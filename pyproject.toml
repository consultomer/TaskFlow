[project]
name = "TaskFlow"
version = "0.1.0"
description = "A task focus dashboard with timer, built with Flask"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "flask>=3.1.3",
    "gunicorn>=21.2.0",
]

[project.scripts]
taskflow = "main:main"
